import { Routes } from '@angular/router';
import { UnknownBookComponent } from './unknown-book.component';

export const UNKNOWN_BOOK_ROUTES: Routes = [
    {path: '', component: UnknownBookComponent},
];