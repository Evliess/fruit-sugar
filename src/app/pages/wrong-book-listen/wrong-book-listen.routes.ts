import { Routes } from '@angular/router';
import { WrongBookListenComponent } from './wrong-book-listen.component';

export const WRONG_BOOK_LISTEN_ROUTES: Routes = [
    { path: '', component: WrongBookListenComponent },
];