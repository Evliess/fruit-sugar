import { Routes } from '@angular/router';
import { ListenWriteComponent } from './listen-write.component';

export const LISTEN_WRITE_ROUTES: Routes = [
    { path: '', component: ListenWriteComponent },
];
