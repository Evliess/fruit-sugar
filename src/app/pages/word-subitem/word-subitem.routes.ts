import { Routes } from '@angular/router';
import { WordSubitemComponent } from './word-subitem.component';

export const WORD_SUBITEM_ROUTES: Routes = [
    { path: '', component: WordSubitemComponent },

]
