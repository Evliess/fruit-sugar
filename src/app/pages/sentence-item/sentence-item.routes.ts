import { Routes } from '@angular/router';
import { SentenceItemComponent } from './sentence-item.component';
export const SENTENCE_ITEM_ROUTES: Routes = [
    { path: '', component: SentenceItemComponent },
];