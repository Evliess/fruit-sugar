server.servlet.context-path: /sugar-dict
audio.dir: ${AUDIO_DIR}
youd.app.key: ${YOUD_KEY}
youd.app.secret: ${YOUD_SECRET}
spring:
  datasource:
    url: ${DB_URL}
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${DB_USER}
    password: ${DB_ACCESS_KEY}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 10
      idle-timeout: 600000
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        temp:
          use_jdbc_metadata_defaults: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    org.hibernate: WARN
    com.zaxxer.hikari: WARN
